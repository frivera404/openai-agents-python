# Production Requirements for MCP Client Application
# Install with: pip install -r requirements.txt

# Core dependencies
openai-agents>=0.0.1
python-dotenv>=1.0.0
click>=8.0.0
requests>=2.28.0

# Optional: For enhanced functionality
# aiohttp>=3.9.0  # For HTTP client features
# pydantic>=2.0.0  # For data validation (if not included in openai-agents)

# Optional providers (uncomment to enable):
# openai>=0.27.0          # OpenAI legacy client
# openai>=1.0.0           # OpenAI new client (if using official package with OpenAI class)
# pinecone-client>=1.0.0  # Pinecone vector database client

# Development dependencies (uncomment for development)
# pytest>=7.0.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# flake8>=6.0.0