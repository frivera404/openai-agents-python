Private repository marker
========================

This snapshot of the repository was marked private on 2026-04-10.

- Commit: cacd895 (short) — created when adding webhook env var support and CLAWCODE forwarding
- Tagged as: `private-cacd895`

If you intend to push this tag to a remote Git host (GitHub/GitLab), run:

```powershell
git push origin --follow-tags
```

Or push just the tag:

```powershell
git push origin private-cacd895
```

Note: Making a repository *private* on a hosting provider is a hosting action (GitHub/GitLab UI or API) and is not performed by this marker file.
